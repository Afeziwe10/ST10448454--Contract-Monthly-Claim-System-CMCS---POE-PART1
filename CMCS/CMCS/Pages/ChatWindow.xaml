<Window x:Class="CMCS.Pages.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMCS.Pages"
        mc:Ignorable="d"
        Title="ChatWindow" Height="600" Width="500" Background="{StaticResource Brush.Surface}" WindowStartupLocation="CenterScreen">
    <Grid>
        <Border Background="{StaticResource Brush.Primary}" Height="60" VerticalAlignment="Top">
            <TextBlock x:Name="ChatTitle" Text="Chat" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" Foreground="White"/>
        </Border>

        <ScrollViewer Margin="10,70,10,80" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="MessagePanel">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{Binding Backgroud}" CornerRadius="12" Padding="10" Margin="5" HorizontalAlignment="{Binding Alignment}">
                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="14"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <DockPanel VerticalAlignment="Bottom" Margin="10">
            <TextBox x:Name="txtMessage" Height="40" FontSize="14" BorderBrush="{StaticResource Brush.Secondary}" BorderThickness="1" Padding="8" Width="392"/>
            <Button Content="Send" Width="80" Margin="10,0,0,0" Background="{StaticResource Brush.Primary}" Foreground="White" Click="Send_Click"/>
        </DockPanel>
    </Grid>
</Window>
