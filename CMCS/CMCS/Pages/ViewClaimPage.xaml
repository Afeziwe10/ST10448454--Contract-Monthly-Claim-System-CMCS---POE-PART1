<Window x:Class="CMCS.Pages.ViewClaimPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:CMCS.Pages"
        mc:Ignorable="d"
        Title="ViewClaimPage" Height="600" Width="900">
    <Grid Background="{StaticResource Brush.Surface}">
        <Border Background="{StaticResource Brush.Primary}" Height="80" VerticalAlignment="Top">
            <TextBlock Text="My Claims" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="26" FontWeight="Bold" Foreground="White"/>
        </Border>

        <DataGrid x:Name="ClaimsGrid" AutoGenerateColumns="False" Margin="40,100,40,40" RowHeight="40" IsReadOnly="True" CanUserResizeRows="False" HeadersVisibility="Column" AlternatingRowBackground="{StaticResource Brush.Secondary}" GridLinesVisibility="Horizontal" BorderBrush="{StaticResource Brush.Primary}" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Claim ID" Binding="{Binding ClaimId}" Width="100"/>
                <DataGridTextColumn Header="Module" Binding="{Binding ModuleName}" Width="*"/>
                <DataGridTextColumn Header="Hours" Binding="{Binding HoursWorked}" Width="100"/>
                <DataGridTextColumn Header="Rate (R)" Binding="{Binding Rate}" Width="100"/>
                <DataGridTextColumn Header="Total (R)" Binding="{Binding TotalAmount}" Width="120"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate}" Width="100"/>
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
